{
  "name": "marketing-openclaw-assistant",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "openclaw gateway --port ${PORT:-8080} --host 0.0.0.0"
  },
  "dependencies": {
    "openclaw": "2026.2.15"
  },
  "engines": {
    "node": ">=22"
  }
}
