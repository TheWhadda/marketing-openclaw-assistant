# ============================================================
# Railway Environment Variables — Marketing OpenClaw Assistant
# ============================================================
# Set these in Railway: your service → Variables
# DO NOT commit a filled-in .env to git.

# --- Required ---

# Password for the /setup wizard (choose a strong random string)
SETUP_PASSWORD=change-me

# Port (must match Railway Public Networking port — set to 8080)
PORT=8080

# --- Persistence (Railway Volume required, mount at /data) ---

OPENCLAW_STATE_DIR=/data/.openclaw
OPENCLAW_WORKSPACE_DIR=/data/workspace

# --- Security ---

# Admin secret for the Control UI and API access (generate a random string)
OPENCLAW_GATEWAY_TOKEN=change-me

# --- AI Model ---

# Anthropic API key (from https://console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API key (optional — only needed if using an OpenAI model)
# OPENAI_API_KEY=sk-...

# Model (optional). Default: anthropic/claude-sonnet-4-5.
# Anthropic:
#   OPENCLAW_MODEL=anthropic/claude-sonnet-4-5      ← default
#   OPENCLAW_MODEL=anthropic/claude-opus-4-6        ← best quality
#   OPENCLAW_MODEL=anthropic/claude-haiku-4-5-20251001  ← cheapest Anthropic
# OpenAI (set OPENAI_API_KEY above):
#   OPENCLAW_MODEL=openai/gpt-4o-mini               ← cheapest overall
#   OPENCLAW_MODEL=openai/gpt-4o
# OPENCLAW_MODEL=

# --- Telegram Channel ---

# Bot token from @BotFather in Telegram
# Steps: open Telegram → @BotFather → /newbot → copy the token
TELEGRAM_BOT_TOKEN=123456789:AA...

# Access control: comma-separated list of Telegram usernames or numeric user IDs.
# When set → only listed users can DM the bot (allowlist mode).
# When unset → anyone can DM the bot (open mode).
# Get your user ID: message @userinfobot in Telegram.
# Examples:
#   TELEGRAM_ALLOWLIST=@ivan,@maria,@alex
#   TELEGRAM_ALLOWLIST=123456789,987654321
# TELEGRAM_ALLOWLIST=
